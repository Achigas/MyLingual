    <div>
        <h1 class="text-center font-italic">Let's learn together!</h1>
        <p class="text-center lead">MyLingual is a community for people learning the same language to meet and share. All levels welcome!</p>
      </div> 
    
    <br>
    
<div class="row justify-content-center">
    <div class="card" style="width: 55rem;">
        <h5 class="card-header">See what everyone is sharing!</h5>
        <div class="card-body">

          <ul class="post-list">
            {{#each posts as |post|}}
            <li>
              {{> post-info post }}
            </li>
            {{/each}}
          </ul>

      </div>
    </div>
  </div>

