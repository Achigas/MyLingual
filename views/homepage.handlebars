    <div>
        <h1 class="text-center font-italic">Let's learn together!</h1>
        <p class="text-center lead">MyLingual is a community for people learning the same language to meet and share resources. All levels welcome!</p>
      </div> 
    
    <br>
    
<div class="row justify-content-center">
    <div class="card" style="width: 55rem;">
        <h5 class="card-header">See what everyone is sharing in the language you are learning!</h5>
        <div class="card-body">
          <ol class="post-list">
            {{#each posts as |post|}}
            {{#if (isCurrentLanguage ../languageid user.language_id)}}
            <li>
              {{> post-info post }}
            </li>
           {{/if}}
          {{/each}}
          </ol>

      </div>
    </div>
  </div>

