 <div>
        <h1 class="text-center font-italic">Meet People</h1>
          <p class="text-center lead">Connect with people learning the same language as you.</p>
    </div> 

<br>
<div class="row justify-content-center">
    <div class="card" style="width: 45rem;">
        <h5 class="card-header">Say hello to other learners!</h5>
        <div class="card-body">

          <ol class="user-list">
            {{#each users as |user|}}
            <li class="meet-user">
             Username: <span>{{username}}</span><br>
             Learning Language: <span>{{language.language_name}}</span>  
            </li>
            {{/each}}
          </ol>

      </div>
    </div>
  </div>